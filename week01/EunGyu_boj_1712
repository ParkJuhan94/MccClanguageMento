#include <stdio.h>

int main() {
	int A, B, C, price;

	scanf("%d %d %d", &A, &B, &C);
	if (B >= C) {
		printf("-1");
		return 0;
	}
	price = A / (C - B) + 1;

	if (price <= 0) {
		printf("-1");
	}
	else
		printf("%d", price);
	
}
